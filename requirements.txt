# Daily Driver CAD toolchain — core deps to BUILD the parts (build.py).
# Python 3.10-3.12 recommended (CadQuery's OCP wheels track these closely).
# For the live VS Code OCP CAD Viewer, also: pip install -r requirements-dev.txt
cadquery>=2.4

# AI-assisted design pipeline (Stages 1-2): official FAL Python client.
# Package is `fal-client`; import is `fal_client`. Reads FAL_KEY from env.
fal-client>=1.0,<2

# Optional, for visual inspection while iterating:
# cq-editor      # GUI viewer
# pip install git+https://github.com/CadQuery/cq-editor.git
